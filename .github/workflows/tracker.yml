
name: GamerPower Telegram VIP Final by DevChaggy0x1

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gamerpower-telegram-vip-final
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run with Node 24-ready actions
        run: echo "Runner ready"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          OWNER_TELEGRAM_ID: ${{ secrets.OWNER_TELEGRAM_ID }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: python bot.py

      - name: Commit state if changed
        if: always()
        run: |
          git config user.name "DevChaggy0x1"
          git config user.email "devchaggy0x1@users.noreply.github.com"
          git add state.json
          git diff --cached --quiet || git commit -m "Update VIP final bot state"
          git push
